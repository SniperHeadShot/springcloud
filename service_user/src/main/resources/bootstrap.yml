# 配置 端口
server:
  port: 8900

eureka:
  instance:
    prefer-ip-address: true # 使用IP注册
    instance-id: ${spring.cloud.client.ip-address}:${server.port}

# 配置中心配置
spring:
  application:
    name: service-user
  profiles:
    active: dev
  cloud:
    config:
      label: master
      profile: user
      uri: http://101.132.45.166:8686/
      discovery:
        enabled: true
        service-id: service-config

# rabbitmq 配置
  rabbitmq:
    host: 101.132.45.166
    port: 5672
    username: yushen
    password: 123123

# redis 配置
  redis:
    host: 101.132.45.166
    port: 6379
    password: zyagmx1314
    database: 1
    jedis:
      pool:
        max-active: 8
        max-idle: 500
        max-wait: -1ms

