# 配置 端口
server:
  port: 8900

# 配置中心配置
spring:
  application:
    name: service-user
  profiles:
    active: dev
  cloud:
    config:
      label: master
      profile: user
      uri: http://101.132.45.166:8686/
      discovery:
        enabled: true
        service-id: service-config
# rabbitmq 配置
  rabbitmq:
    host: 101.132.45.166
    port: 5672
    username: yushen
    password: 123123